/** Identity a fresh deployment starts with, before a Discord server is connected. */
export const defaultWorkspaceName = "Workspace";
export const defaultWorkspaceSlug = "workspace";

/** Slugs are shown as `<slug>.tasks` and validated against this length. */
export const workspaceSlugMaxLength = 40;
